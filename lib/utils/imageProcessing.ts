// 클라이언트 리사이즈/압축(HEIC 변환 포함, 기획서 v1.6 3.1)을 시도했으나, 실제 저사양/보급형
// 안드로이드 기기에서 createImageBitmap 디코딩 자체가 브라우저 탭을 통째로 크래시시키는
// "메모리가 부족하여..." 오류를 반복적으로 일으켰다. 이건 JS 예외가 아니라 탭/렌더러 프로세스가
// 죽는 것이라 try/catch로 복구가 안 된다 (실제로 try/catch로 감싸도 재발 확인함).
//
// 그래서 지금은 원본을 그대로 업로드한다 — 느리고 용량이 크지만 최소한 등록은 막히지 않는다.
// 리사이즈/압축은 서버 쪽(업로드 API나 별도 이미지 처리 서비스)에서 하는 방식으로 나중에 다시
// 붙이는 걸 검토한다.
export async function prepareProductPhoto(file: File): Promise<File> {
  return file;
}
